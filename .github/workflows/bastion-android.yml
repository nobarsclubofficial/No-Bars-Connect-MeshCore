name: Bastion Android APK

on:
  push:
    branches: [bastion]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-android:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze diagnostic entrypoint
        run: flutter analyze lib/bastion_bootstrap.dart

      - name: Build Bastion diagnostic APK
        run: flutter build apk --release --target lib/bastion_bootstrap.dart

      - name: Stage APK
        run: |
          mkdir -p dist
          cp build/app/outputs/flutter-apk/app-release.apk dist/Bastion-Build1-Diagnostic-arm64.apk

      - name: Upload Bastion APK
        uses: actions/upload-artifact@v4
        with:
          name: Bastion-Build1-Diagnostic-APK
          path: dist/Bastion-Build1-Diagnostic-arm64.apk
          if-no-files-found: error
          retention-days: 30
